{#def #}
{# Block-form set so the capture is Markup and survives autoescaping #}
{% set cally_script %}<script type="module" src="https://cdn.jsdelivr.net/npm/cally/dist/cally.js"></script>{% endset %}
<shared.PageLayout title="Audit Log" scripts_extra={{ cally_script }}>
  <div class="max-w-6xl mx-auto p-4 flex flex-col gap-4">
    <shared.layout.CompanyChrome active="audit_log" />
    <h1 class="sr-only">Audit Log</h1>
    <shared.cards.AuditLog show_filters={{ true }} page_size={{ 25 }} title="Audit Log" />
  </div>
</shared.PageLayout>
