{# def character, campaign #}

<div class="flex flex-wrap items-center gap-x-4 gap-y-1 py-2 sm:flex-nowrap">
  <a href="{{ url_for('character_list.index', campaign_id=campaign.id) }}"
     class="btn btn-ghost btn-md">
    <i class="fa-solid fa-chevron-left"></i>
    <span class="uppercase font-thin tracking-wider">Characters</span>
    <span class="font-semibold truncate">{{ character.name_full }}</span>
  </a>
</div>
